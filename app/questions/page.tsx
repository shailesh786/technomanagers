/**
 * app/questions/page.tsx — Questions listing  (/questions)
 *
 * Rendering: SSR (React Server Component)
 *
 * Phase 2: Replace this stub with the migrated <Questions /> component.
 * The RSC will:
 *  1. Fetch questions, categories, and companies from Supabase server-side
 *  2. Pass initial data as props to <QuestionsClient /> (interactive filters, etc.)
 *
 * SEO: generateMetadata will be added in Phase 5.
 */

export default function QuestionsPage() {
  return (
    <div className="container py-20 text-center">
      <h1 className="text-4xl font-heading font-bold">
        🚧 Questions — Phase 2 migration pending
      </h1>
      <p className="mt-4 text-muted-foreground">
        This page will be migrated in Phase 2.
      </p>
    </div>
  );
}
