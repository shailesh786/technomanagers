import { createHubOgImage, OG_CONTENT_TYPE, OG_SIZE } from '@/lib/og-image';

export const size = OG_SIZE;
export const contentType = OG_CONTENT_TYPE;
export const alt = 'Role interview questions on Technomanagers';

export default createHubOgImage('role');
