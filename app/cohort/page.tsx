/**
 * app/cohort/page.tsx — Cohort programme page (Server Component shell)
 */

import CohortPage from '@/components/cohort/CohortPage';

export default function Cohort() {
  return <CohortPage />;
}
