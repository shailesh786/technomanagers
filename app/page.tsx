/**
 * app/page.tsx — Home page  (/)
 *
 * Rendering: SSR (React Server Component)
 *
 * Phase 2: Replace this stub with the migrated <Home /> component.
 * The RSC will fetch featured questions and hero data server-side, then
 * pass them as props to the interactive client sub-components.
 *
 * SEO: generateMetadata will be added in Phase 5.
 */

export default function HomePage() {
  return (
    <div className="container py-20 text-center">
      <h1 className="text-4xl font-heading font-bold">
        🚧 Home — Phase 2 migration pending
      </h1>
      <p className="mt-4 text-muted-foreground">
        This page will be migrated in Phase 2.
      </p>
    </div>
  );
}
